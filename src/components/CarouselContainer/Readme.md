# CarouselContainer

Capture boolean input from user

```js
import CarouselContainer from './CarouselContainer'; // or import { CarouselContainer } from "@components"
```

#### props 

| Name     | Type                                                                                      | Description             |
|----------|-------------------------------------------------------------------------------------------|-------------------------|
| children | (ReactChild[] & (boolean | ReactChild | ReactFragment | ReactPortal | null)) | undefined  | CarouselContainer label |